foo:fn<T>(list<T>) -> T
-- Expression type --
fn(list<T>) -> T
-- Note --
A binder is in scope for everything below it, so a type variable is a type wherever a type can go. These cases put one
in every place a type is written.
