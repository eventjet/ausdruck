names:list<string>.withFallback(2, Some(7))
-- Functions --
withFallback: fn<T, U>(list<T>, U, Option<U>) -> { item: T, fallback: U }
-- Expression type --
{ item: string, fallback: int }
-- Note --
The explicit constructor confirms the type inferred from the earlier argument.
